---
phase: 02-import-library
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - components/ui/alert-dialog.tsx
  - components/ui/progress.tsx
  - components/library-row.tsx
  - components/import-trigger.tsx
  - app/page.tsx
autonomous: true
requirements: [LIBR-01, LIBR-03, LIBR-04, LIBR-05]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- Goal-backward (phase goal + ROADMAP success criteria 4, 5) ---
    - "Each library row shows a real progress bar whose fill is derived from the book's stored position and duration (LIBR-04)"
    - "Swiping a library row left reveals a delete action; the row has no permanently-visible delete icon at rest (D-07)"
    - "Tapping the revealed delete action shows the confirmation 'Delete book: Remove {title} and free its storage? This can't be undone.' before anything is deleted (D-08, locked verbatim)"
    - "Confirming the delete removes the book and its audio bytes, and the row disappears from the list immediately with no manual refresh (LIBR-05)"
    - "Cancelling the confirmation closes the dialog, resets the swipe-revealed panel to hidden, and deletes nothing"
    - "Once the library holds at least one book, a Plus import trigger renders top-right in the header so more books can be imported (02-UI-SPEC.md Layout)"
    # --- Edge Coverage lift (spec-less probe fallback, §C: covered -> plain truth) ---
    - "LIBR-01 adjacency: library row layout, spacing between rows, and visual adjacency follow 02-UI-SPEC.md's Layout section — 16px horizontal padding matching the header, 8px vertical gap between rows, single-column vertical stack, no virtualization"
    - "LIBR-03 adjacency: the title's placement relative to the other row elements (progress line, delete action) follows 02-UI-SPEC.md's Layout section"
    - "LIBR-03 empty: N/A — a title only exists on a row that exists; there is no standalone empty-title state"
    - "LIBR-04 adjacency: the progress bar and the percent/time-remaining line sit directly below the title, in that order, per 02-UI-SPEC.md's Layout section"
    - "LIBR-04 empty: N/A — progress only exists on a row that exists"
    - "LIBR-04 ordering: N/A — the progress display has no independent order"
    - "LIBR-05 idempotency: db.books.delete(id) is idempotent — deleting an already-deleted or nonexistent id is a safe no-op rather than an error, and useLiveQuery re-renders immediately after a successful delete"
    - "LIBR-05 concurrency: delete is a single independent Dexie operation per book id; no other operation in this phase writes to an existing book's Blob or metadata after import completes, so no concurrent-write race exists to guard against"
    # --- 02-UI-SPEC.md `## UI Considerations` lift (identical rule as Edge Coverage) ---
    - "UI populated / library-list: one row per book — Secondary-surface card with title, progress bar, percent/time-remaining line, and swipe-to-delete"
    - "UI overflow / library-row title: filename-derived titles can be arbitrarily long — single-line truncation with CSS ellipsis, never wrapping, with the full text available to assistive tech via the row's aria-label"
    - "UI long-text / library-row title: same resolution as overflow — truncation, not wrapping, and no tooltip since this is a touch device with no hover state to rely on"
  artifacts:
    - path: "components/ui/alert-dialog.tsx"
      provides: "Radix-backed alert dialog primitives styled to the locked tokens"
      exports: ["AlertDialog", "AlertDialogTrigger", "AlertDialogContent", "AlertDialogHeader", "AlertDialogFooter", "AlertDialogTitle", "AlertDialogDescription", "AlertDialogAction", "AlertDialogCancel"]
    - path: "components/ui/progress.tsx"
      provides: "Radix-backed progress bar styled to the Accent token"
      exports: ["Progress"]
    - path: "components/library-row.tsx"
      provides: "One book: title, progress bar, percent/time-remaining, swipe-to-delete, confirmation"
      contains: "db.books.delete"
    - path: "components/import-trigger.tsx"
      provides: "Shared file-picker trigger for the empty-state copy and the header Plus button"
      contains: "type=\"file\""
  key_links:
    - from: "components/library-row.tsx"
      to: "components/ui/alert-dialog.tsx"
      via: "delete confirmation dialog carrying the D-08 copy verbatim"
      pattern: "AlertDialogDescription"
    - from: "components/library-row.tsx"
      to: "lib/db.ts"
      via: "db.books.delete(id) on confirm, removing bytes and metadata in one call"
      pattern: "db\\.books\\.delete"
    - from: "components/library-row.tsx"
      to: "lib/format.ts"
      via: "percentComplete and formatTimeRemaining drive both the bar value and the text line"
      pattern: "formatTimeRemaining"
    - from: "app/page.tsx"
      to: "components/import-trigger.tsx"
      via: "same trigger component rendered in both the empty state and the populated header"
      pattern: "ImportTrigger"
  prohibitions:
    - statement: "MUST NOT leave a book's audio bytes behind in IndexedDB when its row is removed from the list — 'disappears from the library' and 'frees its storage' are one operation, never two, and a delete that only removes metadata silently keeps consuming hundreds of megabytes the user believes they reclaimed"
    - statement: "MUST NOT delete a book without the user first confirming it through the locked D-08 confirmation; no swipe distance, timing, gesture, or undo-toast pattern may substitute for that explicit confirmation step"
    - statement: "MUST NOT display a progress percentage, bar fill, or time-remaining value that is not derived from the book's real stored position and duration"
---

<phase_goal>
**Phase goal (ROADMAP, verbatim):** User can import their own audiobook files and manage a persistent, on-device library of them.

**This plan's slice:** the library row becomes fully real — a true progress bar, a properly truncating title, and swipe-to-delete that actually frees the storage.
</phase_goal>

<objective>
Expand the tracer's inline row into a real, managed library row: the shadcn-shaped `Progress` bar for LIBR-04, single-line title truncation with an accessible full title, native-feeling swipe-to-delete revealing a Destructive action, the locked D-08 confirmation dialog, and a `db.books.delete` that removes the audio bytes along with the metadata. Also add the header `Plus` import trigger that the populated state needs, since the empty-state copy is no longer on screen once books exist.

Purpose: delete is the last of the five ROADMAP success criteria and the only destructive operation in the app — it needs the real Radix dialog, not a browser confirm, and it must free storage rather than orphan it.

Output: two hand-vendored `components/ui/*` primitives, `components/library-row.tsx`, `components/import-trigger.tsx`, and a slimmed `app/page.tsx` that composes them.
</objective>

<execution_context>
@/home/user/neoancestral/.claude/gsd-core/workflows/execute-plan.md
@/home/user/neoancestral/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-import-library/02-CONTEXT.md
@.planning/phases/02-import-library/02-RESEARCH.md
@.planning/phases/02-import-library/02-PATTERNS.md
@.planning/phases/02-import-library/02-UI-SPEC.md
@.planning/phases/02-import-library/02-01-SUMMARY.md
@components.json
@app/globals.css
</context>

<environment_constraints>
**`ui.shadcn.com` is blocked by this environment's outbound proxy** — a `CONNECT` to it returns 403. Confirmed twice: Phase 1's `shadcn@2 init` failed on exactly this (`01-01-SUMMARY.md`, "Manual lucide-react install after shadcn init's registry fetch was blocked"), and it was re-tested live at plan time for this phase.

Therefore `pnpm dlx shadcn add alert-dialog` and `pnpm dlx shadcn add progress` **cannot succeed here**, and Task 1 does not depend on them. `registry.npmjs.org` is reachable (HTTP 200, and it is in the proxy's `noProxy` list), so the underlying Radix primitives install normally and the component wrappers are hand-authored.

**This is not a scope reduction.** The deliverable is unchanged: real Radix-backed `alert-dialog` and `progress` components living at `components/ui/*` behind the `@/components/ui` alias `components.json` already declares, styled to the locked tokens. Only the acquisition mechanism changes, because the registry host is unreachable.
</environment_constraints>

<claude_discretion>
| # | Decision | Basis |
|---|---|---|
| 1 | **No `lib/utils.ts` / `cn()` helper, and no `clsx` or `tailwind-merge` dependency.** The two vendored components compose class strings directly. | `clsx` and `tailwind-merge` are absent from 02-RESEARCH.md's Package Legitimacy Audit (which assumed the shadcn CLI would pull them in transitively). Under the package-legitimacy fallback policy an unaudited package is `[ASSUMED]` and needs a blocking human checkpoint before install. Avoiding both keeps this phase's dependency surface to exactly the four **Approved** packages and directly serves 02-RESEARCH.md's own supply-chain mitigation ("keep the dependency surface minimal"). Neither component needs conditional class merging. |
| 2 | Vendored components omit shadcn's usual `buttonVariants` import for the dialog's action and cancel buttons. | That import would require `components/ui/button.tsx` plus `class-variance-authority`, another unaudited package. 02-UI-SPEC.md specifies the exact appearance of both buttons anyway, so the classes are written directly. |
| 3 | Swipe threshold of 48px to latch the revealed panel, panel width 96px. | 02-UI-SPEC.md specifies the panel's appearance but not its dimensions. 96px comfortably holds the `Trash2` icon plus the "Delete" label at a 44px-plus tap target. |
</claude_discretion>

<tasks>

<task type="auto">
  <name>Task 1: Vendor the Radix alert-dialog and progress primitives into components/ui</name>
  <precondition>`registry.npmjs.org` is reachable — `pnpm view @radix-ui/react-alert-dialog version` returns a version string. If it does not, stop: this task cannot install its dependencies.</precondition>
  <read_first>
    - `components.json` — confirms the `@/components/ui` and `@/components` aliases and the `new-york` / `neutral` / `cssVariables` preset the vendored files must match
    - `app/globals.css` — the locked color, typography and spacing tokens
    - `.planning/phases/02-import-library/02-UI-SPEC.md` — "Delete Confirmation Dialog" and "Library Row — Populated State" for the exact surface, text and fill colors these two components must render
    - `.planning/phases/02-import-library/02-RESEARCH.md` — Standard Stack (verified Radix versions) and the Package Legitimacy Audit
    - `.planning/phases/01-install-offline-app-shell/01-01-SUMMARY.md` — the recorded shadcn-registry block and how Phase 1 worked around it
  </read_first>
  <files>package.json, pnpm-lock.yaml, components/ui/alert-dialog.tsx, components/ui/progress.tsx</files>
  <action>
Install the two Radix primitives with `pnpm add @radix-ui/react-alert-dialog @radix-ui/react-progress`, resolving to the versions 02-RESEARCH.md verified against the live registry: `@radix-ui/react-alert-dialog@1.1.23` and `@radix-ui/react-progress@1.1.16`. Re-verify with `pnpm view` first, per that audit's re-verify instruction. Install nothing else — these two complete the four-package budget 02-RESEARCH.md's audit approved for this phase.

Optionally attempt `pnpm dlx shadcn@2 add alert-dialog progress` first for provenance. It is **expected to fail** with a 403 on the registry host, exactly as Phase 1 recorded. Do not retry it, do not try a different CLI major version, and do not treat the failure as a blocker — proceed straight to hand-authoring.

Hand-author `components/ui/alert-dialog.tsx` as a client component wrapping `@radix-ui/react-alert-dialog`, following shadcn's standard new-york composition. Export `AlertDialog`, `AlertDialogTrigger`, `AlertDialogPortal`, `AlertDialogOverlay`, `AlertDialogContent`, `AlertDialogHeader`, `AlertDialogFooter`, `AlertDialogTitle`, `AlertDialogDescription`, `AlertDialogAction` and `AlertDialogCancel`, each forwarding refs and spreading props onto its Radix counterpart. Compose class strings with plain template literals so a caller-supplied `className` is appended to the defaults — there is no class-merging helper in this project and none is being added.

Style it to 02-UI-SPEC.md's Delete Confirmation Dialog section: the overlay dims the Dominant background and covers the viewport; the content surface uses the Secondary color `#171717` with the same 8px rounding as a library row, is centered and horizontally inset, and carries 16px padding; the title uses Heading typography (20px, 600 weight, 1.2 line height) in Foreground `#F5F5F5`; the description uses Body typography (16px, 400, 1.5) in Foreground; the footer lays the two buttons out with an 8px gap. `AlertDialogAction` renders on a `#DC2626` Destructive background with `#F5F5F5` text; `AlertDialogCancel` renders on the Secondary surface with no accent. Both buttons carry Label typography and a minimum 44px tap height. Use raw hex arbitrary-value Tailwind classes rather than CSS-variable utilities, matching the Phase 1 precedent documented in 02-PATTERNS.md's Shared Patterns. Do not import a button-variants module — the classes go on these elements directly.

Hand-author `components/ui/progress.tsx` as a client component wrapping `@radix-ui/react-progress`, exporting a single `Progress` that takes a numeric `value` from 0 to 100. Per 02-UI-SPEC.md's Library Row section: the track is 4px tall, full width, rounded, and a subtle near-invisible Dominant-on-Secondary shade; the indicator fills in Accent `#E8B34A` and is positioned by translating it horizontally by the complement of the value, which is the standard Radix progress-indicator technique. This is the first time the Accent token renders on screen anywhere in the app — Phase 1 explicitly reserved it for this.

Do not create `lib/utils.ts` and do not add `clsx`, `tailwind-merge`, or `class-variance-authority`. Note the shadcn-registry block and the hand-vendoring decision in the plan summary so Phase 3 does not rediscover it.
  </action>
  <verify>
    <automated>pnpm build</automated>
    <automated>pnpm lint</automated>
    <automated>node -e "const p=require('./package.json');for(const d of ['@radix-ui/react-alert-dialog','@radix-ui/react-progress'])if(!p.dependencies[d])throw new Error('missing dep '+d)"</automated>
    <automated>node -e "const p=require('./package.json');for(const d of ['clsx','tailwind-merge','class-variance-authority'])if(p.dependencies[d]||p.devDependencies[d])throw new Error('unaudited package installed: '+d)"</automated>
    <automated>grep -c "AlertDialogDescription" components/ui/alert-dialog.tsx</automated>
    <automated>grep -c "@radix-ui/react-alert-dialog" components/ui/alert-dialog.tsx</automated>
    <automated>grep -c "@radix-ui/react-progress" components/ui/progress.tsx</automated>
    <automated>grep -c "#E8B34A" components/ui/progress.tsx</automated>
    <automated>grep -c "#DC2626" components/ui/alert-dialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` and `pnpm lint` both exit 0.
    - `package.json` lists `@radix-ui/react-alert-dialog` and `@radix-ui/react-progress` under `dependencies`, and lists none of `clsx`, `tailwind-merge`, or `class-variance-authority` anywhere.
    - `components/ui/alert-dialog.tsx` exports all nine named alert-dialog parts and imports from `@radix-ui/react-alert-dialog`.
    - `components/ui/progress.tsx` exports `Progress`, imports from `@radix-ui/react-progress`, and renders the Accent hex on the indicator.
    - The Destructive hex appears in `components/ui/alert-dialog.tsx` and nowhere in `components/ui/progress.tsx`.
    - No file imports `@/lib/utils`, and `lib/utils.ts` does not exist.
  </acceptance_criteria>
  <done>Both `components/ui` primitives exist, build, lint, and render the locked Accent and Destructive tokens — without any dependency outside 02-RESEARCH.md's approved four.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extract the real library row and the shared import trigger</name>
  <read_first>
    - `app/page.tsx` — the Plan 02-01 version whose inline row and inline file input are being extracted
    - `lib/db.ts` and `lib/format.ts` — the `Book` shape and the two formatting functions the row consumes
    - `components/ui/progress.tsx` — created in Task 1
    - `.planning/phases/02-import-library/02-UI-SPEC.md` — "Layout", "Library Row — Populated State", "Copywriting Contract", "Spacing Scale" (including the 44px tap-target exception)
    - `.planning/phases/02-import-library/02-PATTERNS.md` — the assignments for `library-row.tsx` and `import-trigger.tsx`, and the icon-usage and client-boundary shared patterns
    - `.planning/phases/02-import-library/02-CONTEXT.md` — D-01 and D-06
  </read_first>
  <files>components/library-row.tsx, components/import-trigger.tsx, app/page.tsx</files>
  <action>
Create `components/import-trigger.tsx` as a client component owning the file input and its two visual forms, so one picker implementation serves both states. It renders a hidden file input carrying the accept hint `audio/mpeg,.mp3,audio/mp4,.m4a,.m4b` and the multiple attribute, and calls a `onFilesPicked` callback prop with the picked file list. A variant prop selects the presentation: the empty-state form wraps arbitrary children in a semantic button with zero visual change from the Phase 1 paragraph — same Body typography and Muted-foreground `#A3A3A3`, no underline, no color shift — with invisible padding extending the hit area to at least 44 by 44 pixels (D-01, and 02-UI-SPEC.md's spacing exception). The header form renders a `Plus` icon at 24px in Accent `#E8B34A` with no border and no background, an `aria-label` of "Import audiobook", and the same 44px minimum tap target via padding. Move the picker logic out of `app/page.tsx` unchanged — this is an extraction, not a rewrite of the behavior Plan 02-01 proved.

Create `components/library-row.tsx` as a client component taking one `Book`. Per 02-UI-SPEC.md's Library Row section: the container uses the Secondary surface `#171717`, 8px rounding, 16px padding, and a 44px minimum height. The title renders in Body typography (16px, 400, 1.5) in Foreground `#F5F5F5` on a single line with CSS ellipsis truncation and no wrapping, because filename-derived titles are arbitrarily long and not developer-controlled. Give the row container an `aria-label` carrying the full untruncated title so assistive tech can reach what the ellipsis hides. Below the title render the `Progress` component from Task 1 with 8px margin above and below, its value computed by `percentComplete` from the book's stored position and duration. Below the bar render the percent-and-time-remaining line in Label typography (14px, 400, 1.5) in Muted-foreground `#A3A3A3`, composed from `percentComplete` and `formatTimeRemaining` in the exact shape 02-UI-SPEC.md locks. Every book in this phase has a stored position of 0, so every bar renders at zero fill and every line reads zero percent for the full duration — that is the correct honest state until Phase 3 advances position, and it must be computed from the stored values rather than short-circuited to a constant.

Rewrite `app/page.tsx` to compose these two components instead of rendering rows and the file input inline. Preserve the three-state branch Plan 02-01 established: an undefined query result renders the header alone with no spinner and no flash of the empty state; an empty array renders the Phase 1 empty state, now with its body copy passed as children to the empty-state form of `ImportTrigger`; a non-empty array renders the row list with 16px horizontal padding matching the header and an 8px vertical gap between rows, in a single column with no virtualization. Render the header form of `ImportTrigger` top-right in the header only when the list is non-empty — the empty state already has its own trigger, and 02-UI-SPEC.md is explicit that the header button is what the populated state needs once the empty-state copy is gone. Keep the `useLiveQuery` ordering from Plan 02-01 exactly as it is.

Import icons directly from `lucide-react` with an explicit `size` prop and an explicit color class, matching the convention already established in this repo. Keep every interactive component marked as a client component. Add nothing about deletion in this task — Task 3 owns it.
  </action>
  <verify>
    <automated>pnpm build</automated>
    <automated>pnpm lint</automated>
    <automated>grep -c "formatTimeRemaining" components/library-row.tsx</automated>
    <automated>grep -c "percentComplete" components/library-row.tsx</automated>
    <automated>grep -c "truncate" components/library-row.tsx</automated>
    <automated>grep -c "aria-label" components/library-row.tsx</automated>
    <automated>grep -c 'aria-label="Import audiobook"' components/import-trigger.tsx</automated>
    <automated>grep -c "useLiveQuery" app/page.tsx</automated>
    <automated>grep -c "orderBy('importedAt').reverse()" app/page.tsx</automated>
    <automated>grep -c "No audiobooks yet" app/page.tsx</automated>
    <automated>grep -c "Import an audiobook to start listening." app/page.tsx</automated>
    <automated>! grep -Eq 'type="file"' app/page.tsx</automated>
    <automated>pnpm test:logic</automated>
    <human-check>Import three books with deliberately long filenames on the installed iPhone PWA. Confirm every title stays on one line with an ellipsis and never wraps or pushes the row taller, the progress bar renders at zero fill in the amber Accent, the line below reads zero percent with a plausible time remaining, and the header Plus button is present now that the library is populated but was absent when it was empty.</human-check>
  </verify>
  <acceptance_criteria>
    - `pnpm build` and `pnpm lint` exit 0, and `pnpm test:logic` still passes.
    - `components/library-row.tsx` computes both the bar value and the text line from `percentComplete` and `formatTimeRemaining` against the book's stored position and duration; no literal percent or duration string is hardcoded in the row.
    - The row title element carries a truncation class, and the row container carries an `aria-label` containing the full title.
    - `app/page.tsx` no longer contains a file input element — the picker lives only in `components/import-trigger.tsx`.
    - `app/page.tsx` still renders both Phase 1 empty-state strings unchanged and still uses the Plan 02-01 `useLiveQuery` ordering.
    - The header `Plus` trigger renders only on the non-empty branch.
  </acceptance_criteria>
  <done>The library renders real, composed rows with a working Accent progress bar and truncating titles, and both import entry points route through one shared trigger component.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Swipe-to-delete with the locked confirmation, freeing the book's storage</name>
  <read_first>
    - `components/library-row.tsx` — the row created in Task 2, which this task extends
    - `components/ui/alert-dialog.tsx` — created in Task 1
    - `lib/db.ts` — confirms the Blob and metadata live in one record, so one delete call removes both
    - `.planning/phases/02-import-library/02-CONTEXT.md` — D-07 and D-08, with D-08's copy locked verbatim
    - `.planning/phases/02-import-library/02-UI-SPEC.md` — "Delete Confirmation Dialog" and the swipe-to-delete paragraph of "Library Row — Populated State"
    - `.planning/phases/02-import-library/02-RESEARCH.md` — the Delete confirmation code example and the Alternatives Considered rationale for Radix over a native confirm
  </read_first>
  <files>components/library-row.tsx</files>
  <action>
Add the swipe gesture and the delete path to the row.

Track horizontal touch movement on the row container with plain touch event handlers — no gesture library is needed or permitted here. Record the starting X on touch start, follow the horizontal delta on touch move, and translate the row's foreground content leftward by that delta clamped between zero and the panel width. On touch end, latch the panel open if the leftward delta passed 48 pixels and otherwise spring it back to closed. Swiping back rightward, or touching anywhere else in the list, closes the revealed panel. Suppress the gesture when the vertical delta dominates the horizontal one, so the swipe never fights the page's own scrolling.

Behind the row, right-aligned and full row height, render the revealed action panel: a `#DC2626` Destructive background 96 pixels wide holding a `Trash2` icon at 20px and a "Delete" label in Label typography, both in Foreground `#F5F5F5`. The panel is the `AlertDialogTrigger` — per D-07 there is no permanently-visible delete icon in the row's resting state, so the trigger only exists once the swipe reveals it.

Wire the trigger to the `AlertDialog` from Task 1. The dialog title is the exact string "Delete book". The description is locked verbatim by D-08 and must be reproduced character for character, with the book's cleaned display title interpolated where the placeholder sits — not the raw filename, and not a paraphrase: `Delete book: Remove '{title}' and free its storage? This can't be undone.` The footer holds a Cancel and a Delete action styled as Task 1 built them.

On confirm, call `db.books.delete` with the book's id. Because the schema co-locates the audio Blob and the metadata in one record, this single call frees the audio bytes along with the row — do not add a second call, and do not delete metadata separately, which would orphan hundreds of megabytes the user believes they reclaimed. The call is idempotent: deleting an already-removed id is a safe no-op, so no existence check is needed. The list updates itself because `useLiveQuery` re-renders on the write; add no manual refetch and no optimistic removal. On cancel, close the dialog and reset the revealed panel to hidden without deleting anything.

Destructive `#DC2626` is used here and only here in this phase — on the revealed panel and on the dialog's confirm action. It must not leak onto the error banner Plan 02-03 adds, which 02-UI-SPEC.md deliberately styles on the Secondary surface instead.
  </action>
  <verify>
    <automated>pnpm build</automated>
    <automated>pnpm lint</automated>
    <automated>grep -c "db.books.delete" components/library-row.tsx</automated>
    <automated>grep -cF "and free its storage? This can" components/library-row.tsx</automated>
    <automated>grep -c "AlertDialogAction" components/library-row.tsx</automated>
    <automated>grep -c "AlertDialogCancel" components/library-row.tsx</automated>
    <automated>grep -c "onTouchStart" components/library-row.tsx</automated>
    <automated>grep -c "Trash2" components/library-row.tsx</automated>
    <automated>! grep -rn "window.confirm" components app lib</automated>
    <automated>pnpm test:logic</automated>
    <human-check>On the installed iPhone PWA with several books imported: swipe a row left and confirm the red panel is revealed without the page scrolling sideways; swipe back right and confirm it closes with nothing deleted. Swipe again, tap Delete, and confirm the dialog reads exactly "Delete book: Remove '{that book's cleaned title}' and free its storage? This can't be undone." Tap Cancel and confirm the book is still there and the panel reset. Repeat and tap Delete, and confirm the row vanishes immediately with no refresh. Then force-quit and relaunch to confirm the deleted book is gone for good and the others survive.</human-check>
  </verify>
  <acceptance_criteria>
    - `pnpm build` and `pnpm lint` exit 0, and `pnpm test:logic` still passes.
    - `components/library-row.tsx` calls `db.books.delete` exactly once, and there is no second deletion call or separate metadata removal anywhere in the file.
    - The dialog description string in the source matches D-08 character for character, with only the title interpolated.
    - The delete action is reachable only through `AlertDialogAction`; no code path calls `db.books.delete` directly from a touch or click handler on the swipe panel.
    - No file in `components/`, `app/` or `lib/` calls `window.confirm`.
    - Touch handlers are present on the row and the revealed panel is 96px wide on the Destructive background.
  </acceptance_criteria>
  <done>A swipe reveals the delete action, the locked D-08 confirmation gates it, and confirming removes the book and its audio bytes with the row disappearing immediately.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Every symbol and file Phase 2 creates or modifies. **Bold** entries are created or modified by this plan.

| Artifact | Kind | Owning plan |
|---|---|---|
| `lib/db.ts` — `db`, `Book` | new — Dexie chokepoint + v1 schema | 02-01 |
| `lib/title.ts` — `cleanTitle` | new — pure transform (D-04/D-05) | 02-01 |
| `lib/duration.ts` — `readAudioDuration` | new — hidden-`<audio>` metadata read | 02-01 |
| `lib/format.ts` — `percentComplete`, `formatTimeRemaining` | new — LIBR-04 progress-line formatting | 02-01 |
| `lib/import.ts` — `ACCEPTED_EXTENSIONS`, `isAcceptedAudioFile`, `importFile` | new — validation + write pipeline | 02-01 (typed `ImportError` added in 02-03) |
| `scripts/library-logic.test.mjs` | new — dependency-free `node:test` gate | 02-01 |
| **`components/ui/alert-dialog.tsx`** | new — hand-vendored Radix alert-dialog | **02-02** |
| **`components/ui/progress.tsx`** | new — hand-vendored Radix progress | **02-02** |
| **`components/library-row.tsx`** | new — row: title, progress bar, swipe-to-delete | **02-02** |
| **`components/import-trigger.tsx`** | new — empty-state tap target + header `Plus` button | **02-02** |
| **`app/page.tsx`** | modified — composes the extracted components | 02-01, **02-02**, 02-03 |
| **`package.json` / `pnpm-lock.yaml`** | modified — adds the two Radix primitives | 02-01, **02-02** |
| `components/import-placeholder-row.tsx` | new — transient importing row (D-02) | 02-03 |
| `components/import-error-banner.tsx` | new — inline dismissible error (D-03) | 02-03 |
</artifacts_this_phase_produces>

<threat_model>
Security enforcement is active at **OWASP ASVS Level 1**, blocking on **high**. In-scope categories for this plan per 02-RESEARCH.md's Security Domain: V8 Data Protection (narrow) and V12 Files and Resources. No V2/V3/V4/V6 surface exists — there is no authentication, session, multi-user, or cryptographic concern in this application.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user gesture → destructive operation | A swipe plus a tap permanently destroys data with no undo and no backup. The confirmation dialog is the boundary control. |
| app → IndexedDB (`lib/db.ts`) | The delete path writes to the same single storage tier the import path does. |
| npm registry → build | Supply-chain boundary — the two Radix primitives are the only network dependency in this plan. |
| shadcn registry → build | **Unreachable** (403). Nothing is fetched from it, which incidentally removes a remote-code-into-repo vector: both `components/ui` files are authored in-repo and reviewable in the diff. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering (destructive, unintended) | `components/library-row.tsx` swipe-to-delete | medium | mitigate | Deletion is unreachable without an explicit confirmation: the swipe only reveals an `AlertDialogTrigger`, and `db.books.delete` is called exclusively from `AlertDialogAction`'s confirm handler. Radix's alert-dialog role additionally traps focus and requires a deliberate choice rather than a dismiss-to-accept. A 48px latch threshold plus vertical-dominance suppression prevents an incidental scroll from revealing the action. |
| T-02-06 | Denial of Service (own storage) | `db.books.delete` | medium | mitigate | Blob and metadata are co-located in one record (Plan 02-01's schema), so one delete frees both atomically. A metadata-only delete would silently strand hundreds of megabytes on a storage-constrained device while the UI reports success. Enforced by an acceptance criterion asserting exactly one deletion call and no separate metadata removal. |
| T-02-03 | Information Disclosure (V8) | IndexedDB at rest | low | accept | Unchanged from Plan 02-01: IndexedDB is unencrypted at rest by browser design and this app cannot change that. Accepted under PROJECT.md's personal-single-device threat model — the user's own files, on their own phone, behind the device passcode. |
| T-02-SC | Tampering (supply chain) | `pnpm add @radix-ui/react-alert-dialog @radix-ui/react-progress` | high | mitigate | Both are **Approved** in 02-RESEARCH.md's Package Legitimacy Audit, resolve to `github.com/radix-ui/primitives`, and declare no `postinstall` script. **The audit's automated verdict was inconclusive, not clean**: the `too-new` flag reflects Radix's very frequent release cadence rather than package age, and every `unknown-downloads` reading is an artifact of `api.npmjs.org` being proxy-blocked in this environment — re-confirmed at plan time. An independent cross-check puts the alert-dialog primitive near 10.9M weekly downloads. Mitigation: install only from `registry.npmjs.org`, pin to the researched versions, add no package outside the audit's approved four (explicitly excluding `clsx`, `tailwind-merge` and `class-variance-authority`, which the audit never covered), and confirm the lockfile diff introduces no unexpected postinstall hook. Per that audit's explicit instruction, no blocking legitimacy checkpoint is inserted for the approved packages. |
| T-02-07 | Tampering (supply chain, avoided) | `components/ui/*` | low | mitigate | Because the shadcn registry is unreachable, no component source is fetched from a remote host and executed — both files are hand-authored and appear in full in the code review diff. |
</threat_model>

<verification>
1. `pnpm build` and `pnpm lint` exit 0; `pnpm test:logic` still passes.
2. All structural gates in each task's `<verify>` block pass, including the assertion that no unaudited package was installed.
3. `pnpm verify:pwa http://localhost:3000` still passes — installability is not regressed.
4. Physical-iPhone checks in Tasks 2 and 3: long-title truncation, Accent progress bar, header `Plus` visibility tied to the populated state, swipe reveal and spring-back, the verbatim D-08 confirmation, cancel leaves the book intact, confirm removes it immediately, and the deletion survives a relaunch.
</verification>

<success_criteria>
- Each row shows a real progress bar plus the percent-and-time-remaining line, both computed from stored values (LIBR-04).
- Long filename-derived titles truncate on one line and stay reachable in full via `aria-label` (LIBR-03).
- Swiping a row reveals a Destructive delete action; no delete icon is visible at rest (D-07).
- The confirmation copy matches D-08 verbatim, and cancelling deletes nothing (D-08).
- Confirming removes the book and its audio bytes in one call and the row disappears immediately (LIBR-05).
- The populated state has a working header import trigger (LIBR-01).
- The phase's dependency surface is exactly the four packages 02-RESEARCH.md approved.
</success_criteria>

<output>
Create `.planning/phases/02-import-library/02-02-SUMMARY.md` when done.
</output>
